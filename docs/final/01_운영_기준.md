# 운영 기준

## 1. 공식 기준 폴더
- 개발 기준: `C:\CODE\2. 보도자료 모니터링(260127 완성본)\NEW\source`
- 실행 기준: `C:\CODE\2. 보도자료 모니터링(260127 완성본)\NEW\release`
- 빌드 산출물 기준: `C:\CODE\2. 보도자료 모니터링(260127 완성본)\NEW\build`
- 문서 기준: `C:\CODE\2. 보도자료 모니터링(260127 완성본)\NEW\docs\final`
- 보관 기준: `C:\CODE\2. 보도자료 모니터링(260127 완성본)\NEW\archive`

## 2. 현재 공식 운영 방식
- 코드 수정은 `NEW\source`에서만 한다.
- 직원용 실행 검증은 `NEW\release`를 기준으로 본다.
- 새 배포본은 `NEW\build`에서 생성한다.
- GitHub 업로드 전에는 `NEW\build` 결과물을 먼저 확인한다.
- 기존 `NEW\docs`는 역사 자료이고, 현재 기준은 `docs\final`만 본다.

## 3. 현재 검증 완료 상태
- `NEW\release\start_tray.bat` 정상
- `NEW\source\setup_source_env.bat` 정상
- `NEW\source\tray_start.bat --check-only`가 source launcher를 가리킴
- `NEW\build\artifacts\v202603111526\MonitoringDashboard.zip` 생성 완료

## 4. 현재 미완료 상태
- GitHub Release 업로드 미실행
- 테스트 설치본에서 `설정 > 업데이트` 실제 반영 검증 미실행
- PyArmor trial 제한 때문에 전체 폴더 암호화는 미사용